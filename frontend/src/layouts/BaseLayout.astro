---
import NavBar from "../components/NavBar.astro";

const { title = "Hiking Trail MVP" } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <style>
      :root {
        --bg: #f5f4ef;
        --surface: #ffffff;
        --text: #1f2a1f;
        --muted: #5b675b;
        --brand: #2f6b3e;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        color: var(--text);
        background: radial-gradient(circle at top, #dce8d7 0%, var(--bg) 55%);
      }

      main {
        max-width: 1100px;
        margin: 0 auto;
        padding: 1rem;
      }

      .panel {
        background: var(--surface);
        border-radius: 14px;
        padding: 1rem;
        box-shadow: 0 10px 30px rgba(24, 36, 24, 0.08);
      }

      @media (min-width: 768px) {
        main {
          padding: 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <NavBar />
    <main>
      <slot />
    </main>
  </body>
</html>
