# 001-trail-campsite-platform Setup Notes

This feature uses a split web architecture:

- `frontend/` for Astro static-first UI
- `backend/` for Fastify REST API

Phase 1 initializes project metadata, environment templates, and lint tooling.